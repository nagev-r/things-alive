export class canvasRenderer{
    constructor (canvas, ctx){
        this.canvas = canvas;
        this.ctx = ctx;
    }


    //add more
    //render single stroke, render all strokes, undo, redo, clear
}